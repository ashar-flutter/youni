import '../../config/barrel.dart';

class Universe extends StatelessWidget {
  const Universe({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(children: [const MainAppBar()]);
  }
}
